import { url } from '@/base'
import { AGENCY } from '@/content'

/**
 * Единственная ссылка на видео во всём проекте.
 *
 * Лежит рядом, а не на чужом CDN. Оригинал весил 11,8 МБ: 1920×1080,
 * десять секунд, из которых нужны 6,8. После обрезки и пережатия
 * в 1280×720 остался 1,0 МБ — в одиннадцать раз меньше, а на фоне
 * под текстом разницы не видно.
 *
 * Ключевые кадры поставлены через каждые 12: пока банк кадров ещё
 * собирается, перемотка идёт по самому файлу, и с редкими опорными
 * кадрами она заметно спотыкается.
 */
export const VIDEO_SRC = url('/mountains.mp4')

/**
 * Обрезка клипа. Файл уже обрезан при пережатии, значения оставлены
 * как есть: они задают отображение прогресса на время и пригодятся,
 * если вернём исходник целиком.
 */
export const VIDEO_START = 0
export const VIDEO_END = 6.8

/** Чернила: читаются на бледных облаках, не спорят с синим акцентом. */
export const INK = '#1D3045'
/** Акцентный синий. */
export const BRAND = '#104FFF'

export interface NavLink {
  label: string
  /** Якорь раздела. Ссылки настоящие: браузер доводит до места сам,
   *  а плавность берётся из scroll-behavior у html. */
  href: string
}

/**
 * Разделы. Первый пункт — название агентства, оно же ведёт наверх.
 * Само название живёт в content.ts (AGENCY), здесь только порядок.
 *
 * Было «Услуги», но раздела с услугами на странице нет — есть «Как
 * работаем». Пункт, ведущий в никуда, хуже честного названия.
 */
export const NAV_LINKS: NavLink[] = [
  { label: AGENCY.name, href: '#top' },
  { label: 'КЕЙСЫ', href: '#cases' },
  { label: 'КАК РАБОТАЕМ', href: '#process' },
  { label: 'КОНТАКТЫ', href: '#contact' },
]

export const CONTACT_EMAIL = AGENCY.email

/**
 * Кривые Lusion. Первая — на появления, вторая — на интерфейс и наведения.
 * Те же значения работают и в JS: ease.lusion / ease.ui из motion/math.
 */
export const EASE = 'cubic-bezier(0.35,0,0,1)'
export const EASE_UI = 'cubic-bezier(0.4,0,0.1,1)'

/** Ниже этой ширины побуквенные анимации выключаем — как и Lusion. */
export const MOBILE_WIDTH = 812
