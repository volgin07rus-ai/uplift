/**
 * Знак UPLIFT.
 *
 * Одна ломаная — вершина с отражением. Заливка взята currentColor, а не
 * зашита белым: знак стоит и в шапке, которая на светлых секциях
 * тёмная, а на тёмных светлая, и в подвале, и на загрузочном экране.
 * Пусть цвет приходит от родителя, как у обычной буквы.
 *
 * Размер тоже задаётся снаружи — через высоту в em, поэтому знак
 * сам подстраивается под кегль строки, рядом с которой стоит.
 */
export function Logo({ className = '' }: { className?: string }) {
  return (
    <svg
      viewBox="0 0 384 194"
      className={className}
      // Рядом всегда стоит слово UPLIFT — читалке знак не нужен
      aria-hidden
      focusable="false"
    >
      <path
        d="M120 73L0 193.5H49L120 121.5L192 193.5L251.5 132.5L181 62.5L192 48.5L335.5 193.5H384L192 0L131.5 62.5L203 132.5L192 143L120 73Z"
        fill="currentColor"
      />
    </svg>
  )
}
